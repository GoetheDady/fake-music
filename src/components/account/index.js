import styled from 'styled-components';

const AccountStyle = styled.div`
  position: absolute;
  right: 0;
  top: 0;
  bottom: 0;
  display: flex;
  align-items: center;
`;

function Account() {
  return (
    <AccountStyle>123</AccountStyle>
  );
}

export default Account;
